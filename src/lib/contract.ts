export class Factory {
    constructor(address: String) {
        this.address = address;  
    }
}

export class Organization {
    address: String

    constructor(address: String) {
        this.address = address;
    }
}

export class Swap {
    address: String

    constructor(address: String) {
        this.address = address;   
    }
}

export class Vault {
    address: String

    constructor(address: String) {
        this.address = address;
    }
}
